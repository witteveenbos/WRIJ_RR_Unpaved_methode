[tool.poetry]
# See for more information on the metadata:
#   https://python-poetry.org/docs/pyproject/
# The version entry must be present, however it is further ignored by the build
# process; the version is automatically set by using ``poetry-dynamic-versioning``.
name = "153961_WRIJ_RR_Unpaved_methode"
version = "0.1.0"
description = "Add a short description here!"
authors = ["harm-nomden-sweco <harm.nomden@sweco.nl>"]
license = "Proprietary - Witteveen+Bos"
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = ">=3.11, <3.12"
hydrolib = "0.5.0"
hkvsobekpy = ">=1.5, <2"
asyncio = ">=4.0.0, <5"
pyogrio = ">=0.9.0, <0.10.0"
gdal = {version = "^3.11.4", source = "wb_pypi"}
fire = "*"
geopandas = ">=1.1.3, <2"
pandas = ">=1.2,<3.0"
rioxarray = ">=0.19.0, <0.20"
imod = ">=1.0.0.post1, <2"
plotly = ">=6.8.0, <7"
jupyterlab-widgets = "3.0.15"

[tool.poetry.group.dev.dependencies]
pylint = "^2.15.10"
pytest = "^7.2.1"

[[tool.poetry.source]]
name = "wb_pypi"
url = "https://pypi.wbad.witteveenbos.com/simple/"
priority = "primary"

[[tool.poetry.source]]
name = "PyPI"
priority = "primary"

[tool.pyscaffold]
# PyScaffold's parameters when the project was created.
# This will be used when updating. Do not change!
version = "4.5"
package = "153961_wrij_rr_unpaved_methode"
extensions = ["wb_simple"]
